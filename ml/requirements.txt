# ML/feature-extraction deps. Reuses the backend pipeline, so install the
# backend requirements too (same OpenCV/NumPy/scikit-image versions).
-r ../backend/requirements.txt

pandas>=2.0
pyarrow>=14.0      # for parquet output (falls back to CSV if missing)
PyYAML>=6.0        # for ml/configs/*.yaml

# Illustration feedback model (build + train + backend inference)
scikit-learn>=1.3
joblib>=1.3
